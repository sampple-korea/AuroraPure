# Aurora Pure 개인정보 안내

[English](PRIVACY.md)

최종 갱신: 2026-09-07

Aurora Pure는 자체 광고, 행동 분석, 사용 통계, 자동 오류 보고 서버를 운영하지 않으며 개인 Google 계정 로그인도 제공하지 않습니다.

## 네트워크로 전달되는 정보

사용자가 검색, 실제 조합 조회, 다운로드를 명시적으로 실행하면 기능 수행에 필요한 범위에서 다음 정보가 Google Play와 외부 익명 연결 서비스에 전달될 수 있습니다.

- 검색어 또는 지정한 패키지명
- Play 제공 파일 선택에 쓰이는 Android API, CPU ABI, 화면 밀도, 기기 프로파일 기능, 로케일 목록
- 앱이 선언한 모든 언어 split을 요청하는 언어 코드
- 서비스가 통신 과정에서 볼 수 있는 네트워크 주소와 일반 프로토콜 정보
- 개인 Google 계정이 아닌 익명 세션 인증 정보

익명 사용은 개인 Google 계정을 입력하지 않는다는 뜻이며 Google과 아무 통신도 하지 않는다는 뜻은 아닙니다. 외부 서비스의 운영과 개인정보 처리는 해당 서비스의 정책에도 영향을 받습니다.

Aurora Pure는 사용자가 동작을 실행한 때에만 조회합니다. 글자마다 원격 자동완성 요청을 보내거나, 설치 앱 목록을 스캔하거나, 백그라운드 카탈로그 요청을 예약하거나, 진단 자료를 자동 업로드하지 않습니다.

## 기기에 남는 정보

- Android 앱은 사용자가 **붙여넣기**를 누른 때에만 클립보드를 읽습니다.
- Android에서 공유받은 Play 링크는 확인 가능한 앱 화면만 열며 자동 다운로드하지 않습니다.
- 임시 APK는 검증·내보내기·취소·정리 전까지 앱 전용 또는 CLI 캐시에 남습니다.
- Android 설정과 작업 기록은 Android 앱 저장소에 남습니다.
- CLI 설정은 운영체제 설정 디렉터리, 기록은 데이터 디렉터리에 저장합니다.
- CLI 기록에는 패키지·버전, 결과 경로와 해시, 크기, 완료 시각, 비밀정보가 아닌 프로파일 ID만 들어갑니다.

인증 토큰, 익명 계정 이메일, 쿠키, 서명된 다운로드 URL, 내부 세션 식별자는 Android 내보내기, `.apks`, CLI 설정, CLI 기록에 저장하지 않습니다. CLI 세션 정보는 메모리에만 존재합니다.

## 내보낸 파일

독립 결과는 원본 `.apk`이고 분할 결과는 루트에 APK 파일만 든 `.apks` ZIP입니다. `download-info.json`, 체크섬 텍스트, 계정 데이터, 토큰, 서명 URL, 추적 식별자를 묶음 안에 넣지 않습니다.

## 삭제와 공유

Android의 **기록만 지우기**와 **저장된 파일 삭제**는 서로 다릅니다. 기록 삭제는 내보낸 파일을 유지합니다. 공유는 완료된 결과에만 허용하며 임시 읽기 권한이 있는 `content://` URI를 사용합니다.

CLI의 `history --clear`는 로컬 기록 파일만 지우며 다운로드한 APK나 `.apks`를 삭제하지 않습니다. 캐시나 부분 파일 삭제는 사용자가 파일 시스템에서 명시적으로 수행합니다.

## Android 권한

Android 플랫폼 권한은 인터넷과 네트워크 상태 권한만 요청합니다. 설치, 전체 앱 조회, 광범위한 저장소, 위치, 연락처, 카메라, 마이크, 알림, 포그라운드 서비스 권한은 요청하지 않습니다. AndroidX가 비공개 호환 브로드캐스트 보호를 위해 앱 ID 범위의 `signature` 권한 하나를 병합하지만, 이는 플랫폼 기능을 부여하지 않으며 같은 인증서로 서명된 앱만 가질 수 있습니다.

실제 동작과 이 문서가 다르다고 판단되면 GitHub Issues로 알려주세요. 진단 자료를 첨부할 때는 토큰, 쿠키, 서명 URL 등 비밀정보를 제거하세요.
